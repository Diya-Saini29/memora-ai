# memora-ai
Personalized AI memory layer. Extracts structured facts from conversations using Gemini, stores with local embeddings (all-MiniLM), retrieves via hybrid search. FastAPI + Streamlit, zero-cost architecture (free APIs + SQLite). Handles multi-valued predicates, soft-delete, dedup.
